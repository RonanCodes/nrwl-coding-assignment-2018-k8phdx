import { Injectable } from '@angular/core';

// TODO: this should be moved out of the ticket modules and into it's own module.

@Injectable({
  providedIn: 'root'
})
export class UsersStoreService {
  constructor() {}
}
